; ModuleID = 'contract_on.ll'
source_filename = "contract_on.ll"

define float @madd(float %a, float %b, float %c) {
entry:
  %m = fmul contract float %a, %b
  %s = fadd contract float %m, %c
  ret float %s
}
