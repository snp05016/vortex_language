<!-- Generated by tools/docs/concepts.py from tools/docs/concepts/concepts.yml; do not edit. -->

???+ remember "Before you start, remember"

    ??? question "Why does incrementing an array element take three instructions on AArch64 but one on x86-64?"

        AArch64 is a load/store architecture, so arithmetic works only on registers: the element must be loaded, incremented and stored back separately; x86-64 is register-memory, so one instruction can read, add to and write the memory operand.

        Introduced in [A1. The machine model](a1-machine-model.md).

    ??? question "What are the small, very fast storage slots inside the processor called?"

        Registers. Instructions mostly work on values held in them.

        Introduced in [Build v0.1: 6. The first machine code](../compiler/guide/stage-6-first-machine-code.md).

    ??? question "What is one basic operation a processor can carry out, such as adding two numbers, called?"

        An instruction. Machine code is a sequence of instructions in numeric form.

        Introduced in [Build v0.1: 6. The first machine code](../compiler/guide/stage-6-first-machine-code.md).
