<!-- Generated by tools/docs/concepts.py from tools/docs/concepts/concepts.yml; do not edit. -->

???+ remember "Before you start, remember"

    ??? question "What does a GPU matrix unit compute in one instruction?"

        D = A times B plus C over one small tile of fixed shape, issued together by a warp, a warpgroup, a SIMD-group or, for the newest instructions, one thread.

        Introduced in [D. Matmul and friends: G11. Matrix units](g11-matrix-units.md).

    ??? question "What is a tile, and why does widening one raise arithmetic intensity?"

        A rectangle of outputs held in a faster memory than its inputs started in, so each input is read once and reused by every output in the tile. A wider tile lets more outputs share each value read, raising operations performed per byte moved without changing the total work.

        Introduced in [D. Matmul and friends: G10. The GPU matmul ladder](g10-matmul-ladder.md).

    ??? question "What is a warp, and how many threads does one NVIDIA warp or Apple SIMD-group hold?"

        A fixed-size group of threads that the GPU issues one instruction to at a time; each thread is one of the warp's lanes. It is 32 threads wide on NVIDIA GPUs and on Apple GPUs.

        Introduced in [A. The machine: G2. The SIMT execution model](g2-simt.md).
