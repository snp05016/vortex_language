<!-- Generated by tools/docs/concepts.py from tools/docs/concepts/concepts.yml; do not edit. -->

???+ remember "Before you start, remember"

    ??? question "What is an add recurrence, and what does it describe?"

        A value that starts at a fixed value in a loop and grows by a fixed step each iteration, where the start or the step may itself be a recurrence of an outer loop. It is how LLVM describes an induction variable.

        Introduced in [The middle end: O8. Loops: structure, induction variables and bounds checks](o8-loops.md).

    ??? question "Which blocks form the natural loop of a back edge?"

        The back edge's target, its header, together with every block that can reach the back edge's source without passing through the header. Every edge from outside the loop enters at the header.

        Introduced in [The middle end: O2. Control-flow graphs and dominance](o2-cfg-and-dominance.md).

    ??? question "What does it mean for block X to dominate block Y, and what does the dominator tree record?"

        Every path from the entry to Y passes through X. Making each block a child of its immediate dominator gives the dominator tree, where X dominates Y exactly when X is Y or an ancestor of Y.

        Introduced in [The middle end: O2. Control-flow graphs and dominance](o2-cfg-and-dominance.md).
