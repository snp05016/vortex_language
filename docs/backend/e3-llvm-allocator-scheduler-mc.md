# E3. LLVM's allocator, scheduler and MC layer

<p class="page-intro">The greedy allocator, the machine scheduler, llvm-mca and the MC layer.</p>

!!! note "This chapter is being written"

    It belongs to the Back end book (E. How LLVM does it). Until it is published,
    the [book overview](index.md) lists every chapter and its status.

    **Builds on:** [E1. The LLVM code generator pipeline](e1-llvm-codegen-pipeline.md), C3 to C6, [B3. Object files and assemblers](b3-object-files.md).
