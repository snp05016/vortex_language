# Concept map

Each box is a concept. Each arrow means "needs this first": the box the
arrow points to needs the box it points from.

The whole site has too many concepts to draw as one useful diagram, so the
map below is split into the same areas as the nav. Pick a tab.

=== "Language tour"

    ```mermaid
    --8<-- "includes/concept-map-tour.mmd"
    ```

=== "Specification"

    ```mermaid
    --8<-- "includes/concept-map-spec.mmd"
    ```

=== "Compiler guide"

    ```mermaid
    --8<-- "includes/concept-map-guide.mmd"
    ```

=== "Back end"

    ```mermaid
    --8<-- "includes/concept-map-backend.mmd"
    ```

=== "Optimize"

    ```mermaid
    --8<-- "includes/concept-map-optimize.mmd"
    ```

=== "GPU"

    ```mermaid
    --8<-- "includes/concept-map-gpu.mmd"
    ```

=== "MLIR"

    ```mermaid
    --8<-- "includes/concept-map-mlir.mmd"
    ```

Some of these are still large. For the neighbourhood of one concept instead
of a whole area, open the page that teaches it. A page with concepts carries
a collapsed "Concept map for this page" section near the end, showing only
what that page introduces, what it needs first, and what needs it.
