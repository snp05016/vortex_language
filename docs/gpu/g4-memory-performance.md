# G4. Memory performance: coalescing and bank conflicts

<p class="page-intro">Coalesced access, shared-memory bank conflicts and vector loads, with transpose as the case study.</p>

!!! note "This chapter is being written"

    It belongs to the GPU book (B. Performance). Until it is published,
    the [book overview](index.md) lists every chapter and its status.

    **Builds on:** [G3. The GPU memory hierarchy](g3-memory-hierarchy.md).
